---
name: visit-prep-generator
description: 基于风神双数据源生成跨境电商商家拜访前情报简报，支持以 seller_name 或 seller_id 为输入，拉取 EU/UK/JP 核心战场数据与 US 对比数据，并将结果输出为独立飞书文档。适用于商家 BD、跨境电商运营、新人首次拜访、老客复访，或需要把商家经营数据快速转成拜访前情报简报的场景。
author: 于奇楠 / Aime
metadata:
  version: "1.3"
  category: "运营工具 / 商家BD"
---

# 商家拜访准备生成器

version: 1.3
## Common Rationalizations

以下想法会导致拜访简报失真，必须回到本 Skill 流程：

- “只看一个市场就能判断商家状态。”
- “US 数据缺失可以凭经验补一个对比结论。”
- “风神字段没核对，先把报告写出来。”
- “没有写后回读也可以声称飞书文档已生成。”
- “商家没有数据时，用漂亮话术替代缺口说明。”

## Red Flags

出现任意信号时停止生成结论，先补齐数据或显式标记缺口：

- 未拿到 `seller_name` 或 `seller_id` 就开始分析。
- EU/UK/JP 与 US 两条数据源未区分用途，或没有用同一商家主键对齐。
- 把 SKU、竞品、预测、ROI 或 CRM 后续动作写成 v1 能力。
- F~J 渠道值没有按“渠道占比 × 日均GMV/K”计算，或缺失值被错误补 0。
- 输出不是新建的独立飞书文档，或仍试图写入固定表格/历史文档。

## Verification

交付前逐项验收：

1. 输入完整：调用时必须提供 `seller_name` 或 `seller_id` 二选一；缺失时先追问，分析周期默认近 6 个月或由用户指定。
2. 双源成立：必须通过 `scripts/fetch_aeolus_batch.py` 拉取双源数据；脚本内部固定调用 `aeolus-platform-analysis`，先查 EU/UK/JP 源 `rid=5466004, sid=2770378`，再查 US 源 `rid=5991071, sid=2770378`；US 无数据时在结果和文档中显式标注 ⚠️。
3. 数据显影：缺失、空值、无法匹配、US 无数据均在简报中明确说明，不用推测替代。
4. 指标口径正确：F~J 渠道值按“对应渠道占比 × 日均GMV/K”计算，保留 2 位小数，缺失留空不填 0。
5. 飞书验收：每次调用都通过 `feishu-doc-writing-guide` 新建一篇独立飞书文档，文档名建议为 `{商家名}_拜访情报简报_{YYYY-MM-DD}`；不得写入固定表格或历史文档。
6. 边界合规：不输出 SKU 级、竞品横向、财务预测、GMV 承诺、拜访后 CRM 闭环。

## 合规默认值

- 输入主键：调用时必须提供 `seller_name` 或 `seller_id`；若两者都缺失，先追问再执行。
- 分析周期：默认近 6 个月。
- US 对比：默认拉取；无数据时输出“⚠️ 暂无 US 风神数据，无法进行标杆对比”。
- 输出形态：每次调用新建一篇独立飞书文档，每个商家一页。
- 文档命名：建议使用 `{商家名}_拜访情报简报_{YYYY-MM-DD}`。
- 历史隔离：技能不维护历史文档，不写入任何固定表格；每次产出互不干扰。
- 文档创建：使用 `feishu-doc-writing-guide`，默认落用户个人空间并保留 RAW 验收。
- 数据校验：可使用 `zero-trust-data-analyzer` 做空值、主键匹配、异常值显影。

## 数据源

- **EU/UK/JP 核心战场：** `https://aeolus-va.tiktok-row.net/pages/dataQuery?appId=555771&dashboardId=511872&id=2476255319&isDefault=1&reportQuerySchemaKey=1108ed5b-0140-4a00-b379-435bddfb7cbf&rid=5466004&sid=2770378&waitForDataReady=0`
- **US 标杆对比：** `https://aeolus-va.tiktok-row.net/pages/dataQuery?appId=555771&rid=5991071&sid=2770378`
- **统一主键：** `global_seller_id`

## 执行流程

1. **确认输入**
   - 获取 `seller_name` 或 `seller_id`（二选一即可）；若两者都缺失，必须先追问。
   - 在任何风神查询或飞书写入前，运行 `python3 scripts/validate_visit_request.py --payload-json '<json>'`；失败即熔断并要求补齐输入。

2. **拉取风神数据**
   - 必须运行 `python3 scripts/fetch_aeolus_batch.py --seller-ids <seller_id...>` 执行批量取数；该脚本内部直接调用内置 `aeolus-platform-analysis` 的 `url_query.py`，禁止临场拼写其他风神查询脚本、SQL 或浏览器抓取链路。
   - 每个 `seller_id` 固定查询两次：EU/UK/JP 源 `rid=5466004, sid=2770378` 作为核心战场画像主来源；US 源 `rid=5991071, sid=2770378` 作为标杆对比来源。
   - 单次查询超时 45s，任一商家超时只在结果中标记 `⚠️ 超时` 并继续下一商家；US 返回 0 行不报错，结果和文档中标注 `⚠️ 暂无 US 风神数据`；所有异常必须写入结果 dict 的 `error` 字段，不得抛出中断整批。
   - 脚本最大并发数固定不超过 3，避免 Aeolus session 排队。

3. **做零信任数据校验与字段换算**
   - 优先检查商家主键是否能稳定匹配两条数据源。
   - 检查商家名称、类目、日均GMV/K、渠道占比、月份等字段是否缺失。
   - 计算 F~J 渠道值时，统一使用“对应渠道占比 × 日均GMV/K”，保留 2 位小数；缺失值留空，不补 0。
   - 可使用 `zero-trust-data-analyzer` 辅助显影空值、重复主键、异常趋势。

4. **形成拜访判断**
   - 先给商家基础画像，再给 EU/UK/JP 核心战场趋势与渠道结构。
   - US 有数据时输出可迁移经验；US 无数据时只说明无法对比，不构造标杆结论。
   - 把结论转化为 3 条拜访建议和 2-3 条推荐话术。

5. **新建独立飞书文档**
   - 使用 `feishu-doc-writing-guide` 每次新建一篇飞书文档，不写入任何固定表格或历史文档。
   - 文档名建议使用 `{商家名}_拜访情报简报_{YYYY-MM-DD}`。
   - 采用 [标准输出模板](references/output-template.md) 的结构，每个商家一页。
   - 表格必须使用飞书支持的 HTML `<table>` 语法；不要使用 Markdown 管道表。

6. **交付与说明**
   - 返回飞书文档链接、数据周期、覆盖商家、缺口列表和验证结果。
   - 如果因风神权限、字段缺失或飞书写入失败无法完成，说明失败点和已完成部分。

## 输出结构

每个商家页面包含：

1. 商家基础画像：名称、类目、GMV 量级、主要市场、入驻时长或可得基础信息。
2. 核心战场表现：EU/UK/JP 月度趋势、渠道结构、关键变化。
3. US 标杆对比：量级、渠道、趋势差异和可迁移经验；无数据则标记 `⚠️`。
4. 拜访建议重点：严格 3 条，按优先级排列。
5. 推荐话术方向：2-3 条，可直接用于开场、追问或资源讨论。
6. 文档头部信息：文档标题遵循 `{商家名}_拜访情报简报_{YYYY-MM-DD}`，并保持单次调用单文档输出。

## 范围边界

v1 支持 seller 级商家情报、EU/UK/JP 核心战场、US 标杆对比、飞书文档、拜访建议与话术。不支持 SKU 级分析、竞品商家横向对比、复杂经营预测、GMV 目标承诺、资源 ROI 计算、拜访后纪要、任务拆解或 CRM 回写。

## 触发词

- 商家拜访准备
- seller_name 拜访简报
- seller_id 拜访简报
- 风神商家画像
- EU/UK/JP 商家表现
- US 标杆对比
- 拜访建议话术

## 案例实录

- 用户输入：

```text
请基于 seller_id 123456 或商家名 HOOMIC，生成拜访前情报简报，并补充 US 对比。
```

- 标准输出：

```text
已新建飞书《HOOMIC_拜访情报简报_2026-07-24》，覆盖商家 HOOMIC（seller_id=123456），周期为近 6 个月。文档包含基础画像、核心战场表现、US 标杆对比、3 条拜访建议和 2-3 条推荐话术；若 US 数据缺口存在，已在对应模块显式标记。
```
