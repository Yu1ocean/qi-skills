# skill-heatmap-generator — 打包忽略清单
# 仅保留 SKILL.md / scripts/ / assets/ / references/ / .aime/ 等核心资产。
__pycache__/
*.pyc
*.pyo

# 运行期产物（每次执行自动重新生成，不进入打包域）
output/

# 历史 _release 产物，由 skill-forge-pipeline 重新生成
_release/
